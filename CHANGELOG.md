# Changelog

## 미공개 (v0.8 2차, PR #14) — 2026-09-24

실제 좌석 조회를 정과장님 PC에서 실제로 해 볼 수 있는 상태까지 정리했다.

- **웹 화면에서 연결 코드를 입력하던 방식을 없앴다.** `2-RailFlow실행.cmd`
  를 누르면 RailFlow 화면이 브라우저에 자동으로 열리고 바로 쓸 수 있다.
  공개 배포본(인터넷 주소)의 자동예약 탭은 이제 설치 방법만 안내한다 —
  인터넷 페이지가 PC 안의 프로그램을 직접 부르는 것은 브라우저가 막기
  때문이다.
- **JSON 파일을 여실 일이 없어졌다.** 공식 화면 위에 안내 띠가 뜨고,
  "이게 열차 행입니다", "여기가 일반실 상태입니다" 를 클릭하시면 된다.
  RailFlow가 같은 화면을 다시 읽어 확인한 뒤에만 연결이 완료된다.
- 연결된 내용은 손으로 고칠 수 없다. 파일을 편집하면 RailFlow가 알아채고
  거부한다. 공식 사이트가 개편되면 엉뚱한 칸을 읽는 대신 멈추고 다시
  연결하라고 알린다.
- **읽기 전용 조회 순서를 바꿨다.** 바로 반복 조회로 들어가지 않고,
  한 번 읽고 → 같은 열차를 한 번 더 읽어 확인한 뒤 → 60초 주기로 넘어간다.
- 일반실·특실 상태를 각각 **화면에 쓰여 있던 글자 그대로** 보여준다.
  조회 시각과 어느 사이트에서 읽었는지도 함께 표시한다.
- 중단 버튼이 60초를 기다리지 않고 바로 먹는다.
- 문제가 생기면 **[진단 요약 만들기]** 로 개인정보 없는 진단 내용을 복사해
  보내실 수 있다.
- 이번 단계에서도 **예약·결제는 하지 않는다.** 예약 기능은 코드 수준에서
  꺼져 있어 실수로도 실행되지 않는다.

## 미공개 (v0.8 1차, PR #14) — 2026-09-24

실제 좌석 조회·예약을 위한 **로컬 자동화 Agent**를 추가했다. 지금까지의
자동예약은 전부 시뮬레이터였고, 이번에 처음으로 실제 공식 예매 화면을
대상으로 하는 코드가 들어왔다.

- 사용자의 Windows PC에서 실행하는 `agent/` 프로그램을 추가했다. 화면이
  보이는 브라우저를 띄우고, 사용자가 **직접 로그인**한 뒤, 고른 열차의 실제
  좌석 상태를 읽는다. 아이디·비밀번호·OTP를 묻거나 저장하지 않는다.
- 자동예약 탭에 **실제 연동** 칸이 생겼다(붉은 테두리로 시뮬레이터와 구분).
  로그인 대기 → 열차 확인 → 좌석 없음 → 좌석 발견 → 예약 승인 대기 →
  예약 성공·결제 필요까지 상태를 그대로 보여준다.
- 처음에는 **읽기 전용**으로만 동작한다. 조건에 맞는 좌석을 찾아도 예약
  버튼을 누르지 않고 멈춘다. 실제 예약은 날짜·구간·인원·좌석등급·열차번호를
  화면에서 다시 확인하고 승인해야 시작된다.
- 예약 버튼은 한 번만 누른다. 누른 뒤에는 **예약내역을 다시 조회해** 예약이
  실제로 생겼는지 확인하고, 확인되지 않으면 다시 누르지 않고 사용자에게
  확인을 요청한다.
- 같은 조건의 예약이 이미 있으면 예약하지 않고 멈춘다. 예약이 확인되면
  나머지 후보 감시를 즉시 중단한다.
- **결제는 자동으로 하지 않는다.** 결제 직전에 멈추고, 화면에서 읽은 실제
  결제기한을 보여준다. 기한을 읽지 못하면 추정값을 만들지 않고 "확인하지
  못했습니다"라고 표시한다.
- 보안문자·대기열·접근 제한·추가 인증·로그인 만료가 나오면 즉시 멈춘다.
  우회하지 않는다. 화면이 바뀌어 열차를 확실히 식별하지 못해도 클릭하지
  않고 멈춘다.
- 실제 사이트 조회는 60초마다 한 번이며, 30초 아래로는 설정할 수 없다.
- **아직 실제 사이트에서 검증하지 않았다.** 공식 예매 화면의 생김새를
  기록하는 준비 단계(`capture`)를 PC에서 한 번 거쳐야 동작한다.


## 미배포 (PR #13) — 2026-09-23

- 검색 결과에서 열차를 골라 자동예약(취소표 감시) 조건으로 바로 넘길 수 있다.
  실제 시간표 결과와 샘플 결과 모두에 "자동예약 후보로 담기"를 추가했고(최대 3편),
  고른 구간·날짜·인원과 열차번호·시각이 감시 등록 폼에 채워진다. 여전히 실제
  좌석 잔여 조회나 예약은 하지 않으며, 선택과 감시 시작을 화면에서 구분한다.
- 검색 조건을 초기화하면 그 조건에서 고른 후보도 함께 정리된다. 이전 조건에서
  고른 후보가 남아 있으면 "이전에 조회한 조건에서 고른 후보"라고 표시한다.
- 하단 메뉴의 아이콘과 글자가 배경 밖으로 밀려나던 문제를 고쳤다.
- 후보 열차 카드에서 체크박스가 카드 전체를 차지하고 열차명·시각·운임·상태
  배지가 카드 밖으로 밀려 나가던 문제를 고쳤다(`/demo`,
  `/demo/booking-automation`). 화면을 좁히거나 200% 확대해도 열차 정보가 남은
  폭을 쓰고, 상태 배지는 필요하면 아랫줄로 내려간다.
- 좁은 화면에서 잘려 보이던 열차번호·역 이름이 이제 줄바꿈되어 전부 보인다.
- 좁은 화면에서 시연 화면 상단의 "RailFlow로 돌아가기"와 "초기화"가 서로 겹쳐
  보이던 문제를 고쳤다.

## v0.2 — 2026-09-19

- 국토교통부 TAGO 열차정보를 호출하는 서버 전용 `RailProvider`를 추가했다.
- 공공데이터 인증키가 없을 때는 기존 데모 결과를 유지하고, 연결 후에는 실제 KTX·SRT 운행시간표로 자동 전환한다.
- 공식 시간표와 데모 정보, 좌석 잔여 상태를 화면에서 명확히 구분한다.
- 실제 시간표 결과에는 코레일 공식 예매 링크를 제공한다.
- 인증키가 브라우저나 소스에 노출되지 않도록 서버 환경변수로만 읽는다.
- 검색 조건 검증, 짧은 결과 캐시, 기본 요청 제한과 오류 상태를 추가했다.
- 서비스 워커가 API 응답을 오프라인 캐시에 저장하지 않도록 수정했다.

### 다음 연결 작업

- 공공데이터포털에서 TAGO 열차정보 활용신청 후 인증키 등록
- 실제 동탄–울산 조회 결과와 역 코드 확인
- 좌석 잔여 조회 및 자동예약은 공식·승인된 연동 방식이 확인된 뒤 별도 구현

## v0.1 — 2026-09-19

- SR 앱의 빠른 모바일 흐름을 참고한 검정/주황 UI를 구현했다.
- 예매, 자동예약, 마이페이지의 3개 탭을 구현했다.
- 데모 열차 검색, 자동예약 등록/중지, 좌석 확보 시뮬레이션을 구현했다.
- 자동예약 목록과 사용자 설정을 브라우저 localStorage에 저장한다.
- 웹앱 매니페스트와 서비스 워커를 추가해 PWA 설치 흐름을 구성했다.
- Galaxy, iPad, 데스크톱에 맞춘 반응형 레이아웃을 구현했다.

### 아직 데모인 기능

- 코레일/SRT 실시간 조회
- 실제 로그인 및 예약
- 백그라운드 반복 조회
- 푸시 알림
- 결제 및 카드 관리
# RailFlow v0.3 — 검증 중, 공개 배포 보류

- 출발/도착역 초기 선택 해제, 검색 가능한 Combobox와 공식 역 코드 목록 API 추가.
- 출발시간 00:00, 전체 초기화 버튼, 조건 변경 시 진행 중인 검색 취소.
- 좁은 화면에서 역/날짜 세로 배치, 입력 폭 제한, 결과와 버튼 줄바꿈.
- 역 목록 24시간 캐시, 도시별 요청 병합, 검색 60초/200개 제한 캐시 및 동시 요청 병합.
- 설정 화면 지연 로딩, 단순 인원 선택을 기본 select로 변경, 서비스 워커의 초기 HTML 중복 요청 제거.
- 인증키 미설정 시 명시적인 연결 준비 안내. 데모는 사용자 선택으로만 진입.
- 노출된 키를 다음 배포용 환경 설정에서 제거. 포털 키 폐기/재발급은 별도 필요.
- 기존 공개 v0.2는 보존. 새 Secret으로 실조회 검증과 Safari 검증 후 v0.3 배포 필요.

### 이어받기 세션 검증 (2026-09-19, 코드 변경 없음)

- 환경 재현(설치·타입검사·lint·모의 계약 테스트·빌드) 통과를 재확인했다.
- 역 도시코드(cityCode)와 실제 역 코드(NAT 접두) 형식을 공개 자료와 교차 확인해 새 키 등록 후 실패할 만한 정적 결함이 없음을 확인했다(간접 확인, 실제 키 호출 성공은 아님).
- Playwright(Chromium)로 iPad 뷰포트 렌더링, Combobox 키보드 선택, 인위적 지연 상황에서의 검색 취소·중복 요청 방지 동작을 실제 브라우저에서 확인했다(Safari 실기기 대체 아님).
- 프로덕션 빌드의 초기 로드 실측치(v0.3 단독 기준선)를 기록했다. v0.2 비교는 v0.2 소스 부재로 여전히 불가능하다.
- 자세한 내용은 docs/V0.3-validation.md의 "이어받기 세션 추가 검증" 참조.

### 이어받기 세션 실행 검증 및 결함 수정 (2026-09-19, 2차)

- 로컬 프로덕션 서버(Cloudflare Worker 프리뷰)를 실제로 띄우고 Playwright(Chromium)로 사용자 동작을 재현해 실제 결함 2건을 발견·수정했다.
  - `app/page.tsx`: 이미 선택된 출발역/도착역을 다른 역으로 바꾸려 클릭하면 커서가 기존 텍스트 끝에 위치해 새 입력이 뒤에 이어붙던 문제를 수정했다(클릭 시 기존 값 전체 선택).
  - `app/globals.css`: 320·375px 등 600px 미만 화면에서 검색/오류 토스트 알림(sonner)이 오른쪽으로 16px 넘치던 문제를 수정했다(sonner 자체 스타일시트의 CSS 제약 충돌, 앱 코드가 아닌 해당 컴포넌트 폭을 좁은 화면에서만 보정).
- 수정 후 타입검사·lint·모의 계약 테스트·빌드를 재실행했고, 25개 실제 브라우저 시나리오(초기 화면, 역 선택, 동일역 차단, 시간 유지, 빈 결과, 오류 배너, 데모 자동대체 없음, 5개 화면 폭)를 전부 재검증했다.
- 캐시 MISS/HIT을 서버 응답 헤더(`server-timing`)로 직접 확인하고, 60초 내 12회 초과 시 429 호출 제한이 실제로 작동함을 확인했다.
- 자세한 내용은 docs/V0.3-validation.md의 "실행 검증 2차 세션" 참조.

### 공개 주소 404 장애 진단 (2026-09-19, 3차, 코드 변경 없음)

- 공개 주소에서 `/api/trains/stations?mode=live`가 404를 반환한다는 사용자 보고를 조사했다. 로컬 개발 서버와 로컬 프로덕션 빌드(`pnpm build && pnpm start`) 양쪽에서 동일한 요청을 재현한 결과 항상 200(데모) 또는 503(`NOT_CONFIGURED`, 실제 모드에서 키 미설정 시)만 응답했고 404는 한 번도 발생하지 않았다.
- 결론: v0.3 소스에는 결함이 없다. 공개 주소가 여전히 v0.2를 실행 중이라 `/api/trains/stations` 라우트 자체가 없어서(v0.2에는 없던, v0.3에서 신설한 라우트) 404가 나는 것으로, GitHub 소스 반영과 ChatGPT Sites 배포는 별개 작업이라는 뜻이다. 코드는 수정하지 않았다.
- 자세한 내용은 docs/V0.3-validation.md의 "공개 주소 404 장애 진단" 참조.

## v0.4 — 안전한 자동예약 기반 (검증 중, PR 병합 전)

**실제 예약 기능은 이번에도 추가되지 않았다.** 이번 작업은 나중에 실제 철도사 Provider를 안전하게 붙일 수 있도록 서버 구조만 만든 것이다.

- 시간표 조회(`RailScheduleProvider`)와 좌석 확보(`RailReservationProvider`)를 별도 인터페이스로 분리했다. 기존 실제/데모 시간표 조회 동작은 전혀 바뀌지 않았다.
- 예약 작업의 12단계 상태(등록→대기열→확인중→예약시도→확보→결제대기→완료, 취소/만료/실패 등)와 전이 규칙, 중복 작업 방지, 만료 후 재진입 차단을 구현했다.
- 서버가 자동예약 작업을 큐에 넣고 한 단계씩 처리하는 Queue/Worker 구조를 추가했다(현재는 테스트·데모용 메모리 구현이며, 서버를 재시작하면 작업이 초기화된다. 실제 운영 저장소는 아직 없다).
- `자동예약` 화면 아래에 "Mock 자동예약 작업(v0.4 서버 기반 미리보기)" 영역을 추가했다. 항상 MOCK 표시와 "실제 예약이 이루어지지 않는다"는 안내가 함께 나타나며, 서버의 예약 기능이 꺼져 있으면(운영 기본값) 안내만 표시된다.
- 개발 환경에서만 보이는 "1~5초 시뮬레이션 간격"은 Queue·Worker·중복방지 테스트용 기능이며, 실제 철도 사이트에 대한 클릭 간격이나 탐지 회피 기능이 아니다. 운영 환경이거나 Mock이 아닌 Provider에서는 서버가 항상 거부한다.
- 실제 코레일·SR 계정 연동, 결제, 자동 로그인, 매크로/탐지회피, 비공개 API 연동은 이번에도 구현하지 않았다. 공식 연동이 확인되기 전까지는 앞으로도 구현하지 않는다.

## v0.5 — Android 중심 취소표 감시 기반 (검증 중, PR 병합 전)

**실제 잔여좌석 조회·예약·결제·코레일 계정 로그인은 이번에도 구현하지 않았다.** RailFlow는 좌석을 감시하고 알릴 뿐이며, 실제 예약은 사람이 코레일+에서 직접 완료한다.

- RailFlow 자체 계정(이메일+비밀번호) 로그인을 추가했다. 코레일 계정과 다르며, 코레일 아이디·비밀번호를 요청하거나 저장하지 않는다. `demo-*` 기반이던 v0.4 예약 작업 데모는 그대로 남아 있고 바뀌지 않았다.
- 자동예약 탭을 "취소표 감시" 화면으로 전면 개편했다: 출발/도착역·날짜·시간범위·열차종류·인원·좌석등급·감시종료시간·알림방식을 등록하면 서버가 감시 작업을 관리한다.
- 공식 잔여좌석 Provider는 아직 없다. 화면과 API 모두 이를 숨기지 않고 `Provider 미연결` 상태로 명확히 표시하며, 실제 좌석을 조회한 것처럼 보이는 화면은 어디에도 없다.
- 좌석 발생 시 알림을 보내는 구조(FCM·Web Push·텔레그램·이메일 Adapter 인터페이스)를 추가했다. 이번 PR은 외부 계정이나 실서비스 키를 요구하지 않는 테스트용 Adapter만 실제로 동작하며, 나머지 채널은 "아직 연결되지 않음"으로 명확히 응답한다.
- "코레일+에서 예매하기" 버튼은 공식적으로 확인된 딥링크가 없어 항상 공식 웹사이트로 연결하고 조건을 복사해준다. 예매가 실제로 완료됐는지는 자동으로 추정하지 않고, 사용자가 "예매 완료" 버튼을 눌러야만 기록된다.
- 개발 환경에서만 보이는 "Mock 취소표 시뮬레이션(검증값 1~5)"은 Queue·Worker·알림 중복방지 테스트용이며, 운영 환경에서는 서버가 항상 거부한다.
- 영구 저장소(PostgreSQL) 스키마를 설계했지만 실제로 연결하지는 않았다 — 서버가 재시작되면 계정과 감시 작업이 모두 사라진다.
- 개인정보처리방침 페이지와 계정 삭제 기능(마이페이지에서 즉시 삭제)을 추가했다.

## v0.6 — 공개 Demo Showcase (검증 중, PR 병합 전)

**실제 잔여좌석 조회·취소표 감시·예약·결제·코레일 로그인은 여전히 구현하지 않았다.** 이번 작업은 그 기능들을 실제로 켜는 대신, 로그인·서버 저장소·실제 철도 API 없이도 전체 흐름을 눌러볼 수 있는 클라이언트 전용 가상 시연 화면(`/demo`)을 추가한 것이다.

- 새 공개 경로 `/demo`(Demo Showcase)를 추가했다. 검색 조건 설정 → 후보 열차 선택 → 가상 감시 등록 → 감시 진행 → 좌석 발견 → 가상 알림 표시 → 공식 예매 안내 → 시연 종료/다시 시작까지 전체 흐름을 로그인 없이 체험할 수 있다.
- Demo Showcase는 `/api/auth/**`, `/api/watch-jobs/**`, `/api/reservations/**`, 코레일·SR·TAGO API, 실제 알림 Provider를 전혀 호출하지 않는다. 상태는 이 브라우저 탭의 `sessionStorage`에만 저장되며(localStorage·쿠키·서버 저장 없음), 새 `lib/demo/**` 모듈로 완전히 분리해 기존 `AuthStore`/`WatchStore`/`SeatAvailabilityProvider`를 전혀 우회하거나 활성화하지 않는다.
- 좌석 발견은 선택한 후보 중 첫 번째에서 결정론적으로(두 번째 감시 단계에서) 일어나며, 나머지 후보는 자동으로 감시 중단 표시된다. 화면 전환 간격(1~5초)은 오직 이 화면의 시연 속도일 뿐 실제 철도 서버 조회 주기가 아니라는 점을 화면에 명시한다.
- 좌석 발견 시 배너·카드 강조·토스트·(지원 기기에서) 짧은 진동으로 알리되, 실제 Push 권한을 요청하거나 FCM/WebPush/텔레그램/이메일을 호출하지 않는다 — "가상 알림입니다. 실제 Push 또는 메시지는 발송되지 않았습니다."를 항상 함께 표시한다.
- "공식 예매로 이동" 버튼은 기존에 쓰던 공식 코레일 웹사이트 주소를 그대로 재사용하며, 검색 조건을 URL로 전달하거나 새로운 비공개 딥링크를 추측하지 않는다.
- 메인 화면과 자동예약(취소표 감시) 화면에 Demo Showcase로 연결되는 안내 링크를 추가했다. 기존 "데모 체험"(시간표 조회 모드 전환) 버튼의 동작은 그대로 유지했다.
- 로그인이 필요한 화면(자동예약/마이페이지)이 실제로 열릴 때만 `/api/auth/session`을 확인하도록 바꿨다 — 예매 탭만 보는 비로그인 사용자는 더 이상 첫 화면 로드에서 불필요한 401을 발생시키지 않는다.
- 실제 운영 인프라(PostgreSQL/Queue/Worker/알림 채널/공식 좌석 Provider) 전환 계획을 `docs/V0.6-OPERATIONS-PLAN.md`에 문서화했다 — 이번 PR은 이 계획을 실행하지 않았고, `SEAT_AVAILABILITY_PROVIDER` 등 기존 fail-closed 기본값은 전혀 바뀌지 않았다.

## v0.7 — 자동 좌석조회·예약 매크로 시뮬레이터 (검증 중, PR 병합 전)

**실제 코레일·SRT 좌석 조회·클릭·로그인·결제는 이번에도 구현하지 않았다.** 이번 작업은 그 대신, RailFlow 자신이 만든 가짜 예매 사이트를 대상으로 실제 브라우저 자동화(Playwright)가 "조건 등록 → 자동 감시 → 좌석 발견 → 자동 클릭 → 자동 예약 → 성공 알림"까지 실제로 동작함을 시연하는 기능이다.

- 새 공개 경로 `/demo/booking-simulator`(Mock 예매 사이트)를 추가했다. 열차 목록·일반실/특실 잔여·매진 여부·구매 버튼·예약 처리·가상 예약번호(`RF-XXXXXXXX`) 발급·결제기한·예약 취소를 서버 상태로 관리하며, 결정론적 시나리오 3개(항상 매진 / 3번째 조회에서 일반실 발생 / 5번째 조회에서 특실 발생)를 시드한다.
- `자동예약` 탭에 실제 서버 기반 매크로 작업 카드(등록 폼 + 진행 상태·좌석 상태·자동 구매클릭 여부·예약 결과·결제기한 카운트다운·중지/재감시/삭제)를 추가했다. 기존 "취소표 감시"(`WatchJobsPanel`)는 그대로 유지되며, 대체되지 않았다.
- 17단계 작업 상태 기계(`SCHEDULED→...→PAYMENT_PENDING→COMPLETED`, 실패/종료 8종)와, 구매 클릭 사실과 예약 성공 사실을 분리 기록해 실제 예약 성공 응답 없이는 절대 "좌석 확보"로 표시하지 않는 원칙을 구현했다.
- 두 서버 프로세스(Worker)가 동시에 같은 작업을 처리해도 예약이 정확히 1건만 이뤄지도록 원자적 스텝 선점(펜싱 토큰)과 이중 idempotency(사이트 자체 + 작업 저장소)를 구현했다. 성공한 후보가 생기면 다른 후보 감시는 즉시(다음 tick부터) 자동 중단된다.
- 자동화의 "1~5초 간격"은 RailFlow 자신의 API를 다시 호출하는 주기일 뿐이며, 실제 외부 철도 사이트에 대한 클릭 간격이나 탐지회피 패턴이 아니다. 그런 목적의 랜덤 지연·패턴 변형 기능도 구현하지 않았다.
- 자동화 Worker가 열 수 있는 주소를 화이트리스트로 강제하는 `host-guard.ts`를 추가했다: `localhost`/`127.0.0.1`와 이 배포 자신의 Vercel 호스트명(`VERCEL_URL`, 앱 설정으로 위조 불가)만 허용되며, 탐색 전/리디렉션 후 두 번 재검증한다. 실제 운영(Production, `VERCEL_ENV`로 판정)에서는 이 값과 무관하게 자동화 기능 전체가 항상 거부된다.
- Playwright는 `data-testid` locator(`train-result`/`seat-status`/`purchase-button`/`reserve-button`/`reservation-result`)만 사용한다 — 좌표 클릭, OCR, 이미지 인식은 쓰지 않는다. 비밀번호·쿠키·세션 토큰·카드정보는 어디에도 기록하지 않는다.
- 실제 코레일·SR 자동화, 비공개 API 연동, CAPTCHA/대기열 우회, 기기·IP 위장, 탐지회피, 실제 결제·카드정보 저장은 이번에도 구현하지 않았으며, 이런 자동화가 실제로 필요해지면 구현하지 않고 중단·보고하는 것을 원칙으로 유지한다.
- Cloudflare Workers 배포 경로(`pnpm build`/`pnpm dev`)는 브라우저 프로세스를 띄울 수 없는 런타임이라, 이 시뮬레이터의 Playwright 자동화는 그 경로에서 실행될 수 없다 — 이 제약과 실제 Android 출시 전 남은 작업·공식 Provider 전환 승인 조건을 `docs/V0.7-AUTOMATION-BOUNDARY.md`, `docs/ANDROID-RELEASE-ROADMAP.md`에 문서화했다.
- 이 검증 과정에서 실제 회귀를 발견해 수정했다: `lib/automation/provider.ts`가 `playwright`를 항상 즉시 import하고 있어, 이 레포의 로컬 개발 서버(Cloudflare Workers 런타임)에서는 자동화 Provider를 기본값(`unavailable`, 운영 환경 포함)으로 둬도 자동화 라우트 전체가 500으로 죽는 문제가 있었다. Playwright(`mock-browser` Provider)를 실제로 선택했을 때만 동적으로 불러오고, 그 로드가 실패하면(이 런타임처럼 애초에 불가능한 경우) 처리되지 않은 크래시 대신 정상적인 오류 응답으로 변환하도록 수정했다.

### 이어받기 세션 — 실제 시연 가능성 검증 및 보완 (2026-09-21, 코드 변경 포함)

- **실제로 로그인이 되는 로컬 Node.js 서버**(이 레포의 Cloudflare Workers 개발 경로가 아니라 `next dev`)에서 실제로 회원가입 → 자동화 작업 등록 → 실제 Playwright 브라우저 자동화 → 좌석 발견 → 자동 구매클릭 → 자동 예약까지 전체 흐름을 처음부터 끝까지 돌려봤고, 그 과정에서 실제 결함을 하나 더 발견·수정했다: `components/automation/booking-simulator.tsx`의 구매 클릭 버튼이 클릭 성공 후 좌석 상태를 다시 불러오지 않아, 화면의 `data-purchase-clicked` 속성이 계속 낡은 값(`false`)에 머물러 있었다. 실제 자동화 Worker(`mock-browser-provider.ts`)는 이 속성이 `"true"`가 될 때까지 기다리므로, 이 결함이 있는 동안에는 **자동 구매클릭이 실제로는 한 번도 성공할 수 없었다**. 클릭 성공 시 상태를 즉시 다시 불러오도록 수정한 뒤, 실제로 가상 예약번호와 결제기한이 발급되는 것까지 확인했다.
- 또 다른 구조적 원인도 확인했다: Vercel Preview 배포는 `next build`로 만들어지는데, Next.js는 `VERCEL_ENV`와 무관하게 이 빌드를 항상 `NODE_ENV=production`으로 만든다. 기존 계정 저장소(`AUTH_STORE`, v0.5)는 `NODE_ENV`로 판정해 Preview에서도 항상 막혀 있었으므로, 지금까지는 Preview에서 회원가입 자체가 불가능했고, 이 때문에 로그인이 필요한 자동예약 탭 전체를 Preview에서 시연할 수 없었다(계정 저장소의 이 정책 자체는 v0.5의 의도적인 설계이며 이번에도 바꾸지 않았다).
- 위 문제를 우회하기 위해 **로그인이 전혀 필요 없는 새 공개 시연 경로 `/demo/booking-automation`**을 추가했다. 서버 계정·자동화 저장소·실제 API를 전혀 쓰지 않는 완전히 독립된 클라이언트 전용 모듈(`lib/automation-demo/**`)이며, 조건 등록 후 "자동 감시 시작"을 누르면 좌석 발견부터 가상 예약 성공까지 자동으로 진행되고 결제 완료만 사용자가 직접 누른다. 새로고침해도 브라우저 `sessionStorage`에서 상태가 복원된다.
- 실제 Playwright 브라우저 자동화가 로드 불가능한 런타임(예: 이 저장소의 Cloudflare Workers 경로)에서도 동일한 시연 시나리오를 안정적으로 보여줄 수 있도록, 브라우저를 쓰지 않는 새 Provider `mock-direct`를 추가했다(RailFlow 자신의 Mock 예매 사이트 저장소를 직접 호출, 외부 요청 없음). `mock-browser` Provider가 실행 불가능한 런타임에서 로드에 실패하면 이제 `RUNTIME_NOT_SUPPORTED`라는 이름으로 명확히 구분되는 오류를 반환한다(이전에는 다른 설정 오류와 같은 코드를 썼다).
- `@playwright/test`를 추가해 실제 브라우저로 두 가지를 검증하는 E2E 테스트를 작성하고 실제로 통과시켰다: (1) 공개 시연(`/demo/booking-automation`)이 조건 등록부터 예약 성공·새로고침 복원·결제 완료까지 실제로 동작하는지, (2) 실제 `mock-browser` Provider 코드가 진짜 Chromium으로 Mock 예매 사이트를 열어 구매·예약을 완료하고, 실제 코레일 도메인으로의 접근은 차단하는지.
- `scripts/verify-automation-demo.cjs`(신규)로 새 공개 시연 모듈의 상태 전이·라운드로빈 좌석 시나리오·경과 시간·sessionStorage 검증·모듈 경계(기존 인증/철도/자동화 코드 미참조)를 오프라인으로 검증했다. `scripts/verify-booking-simulator.cjs`에는 `mock-direct` Provider와 `RUNTIME_NOT_SUPPORTED` 코드에 대한 검증을 추가했다.
- 실제 Vercel Preview 서버리스 함수가 어느 런타임(Node.js/Edge)으로 자동화 API를 서비스하는지, 그리고 Preview에서 `mock-browser`가 실제로 동작하는지는 이번에도 이 세션의 네트워크 제약으로 직접 확인하지 못했다 — `docs/V0.7-PREVIEW-DEMO-AUDIT.md`에 확인한 것과 확인하지 못한 것을 정직하게 구분해 기록했다.

### 검색 결과 → 자동예약 연결 및 하단 메뉴 레이아웃 수정 (2026-09-23)

- **검색 결과에서 자동예약 후보를 고를 수 없던 문제를 고쳤다.** 실제 시간표(TAGO) 결과 카드에는 "공식 예매" 링크만 있고 후보로 담는 동작 자체가 없었다. 이제 실제·데모 결과 모두에 "자동예약 후보로 담기" 버튼이 있고, 최대 3편까지 선택·해제할 수 있다. 선택 목록은 예매 화면에 그대로 보이며 개별 해제·전체 해제가 가능하다.
- **선택한 조건이 자동예약 화면으로 전달되지 않던 문제를 고쳤다.** 예전에는 선택이 localStorage의 별도 목록에만 쌓이고 취소표 감시 등록 폼은 하드코딩된 기본값(`KTX 101` 등)을 그대로 보여줘, 사용자가 조건을 처음부터 다시 입력해야 했다. 이제 선택한 출발역·도착역·날짜·인원과 열차번호·출발/도착시각이 등록 폼에 채워지고, 어떤 후보가 넘어왔는지 화면에 표시된다. 검색 결과의 실제 값만 사용하며 가상 열차를 만들어 끼워 넣지 않는다.
- **검색 조건이 바뀌면 이전 구간·날짜의 후보를 자동으로 해제한다.** 다른 날짜의 후보가 조용히 남아 있지 않는다.
- **하단 메뉴 배지가 "등록된 작업 수"처럼 보이던 표시를 고쳤다.** 이 숫자는 아직 선택만 한 후보 수이며, 선택과 실제 감시 시작을 화면에서 구분해 안내한다. 선택 시점에는 어떤 서버 작업도 만들어지지 않고, 로그인·저장소·Provider가 준비되지 않은 상태에서는 기존 안내(로그인 필요/등록 기능 준비 중)가 그대로 나타난다. **실제 잔여좌석 조회·자동예약 Provider는 이번에도 구현하지 않았다.**
- **하단 메뉴의 아이콘·글자가 배경 밖으로 밀려나던 문제를 고쳤다.** 원인은 두 가지였다: (1) 탭 목록의 기본 스타일에 있는 높이(36px)가 의도한 높이 지정을 덮어써 메뉴 배경이 실제로 36px밖에 되지 않았고, (2) 전역 규칙 `main button { height:auto; min-height:44px }`가 탭 버튼의 높이 지정을 덮어써 버튼이 44px가 되면서 36px짜리 배경 밖으로, 그리고 화면 아래로 삐져나갔다. 배경 높이를 내용에 맞게 늘리고 버튼 최소 높이를 56px로 잡아 아이콘과 라벨이 모두 배경 안에 들어가게 했다. 글자를 줄이거나 잘라내지 않았고, 모바일 하단 안전영역(safe-area) 여백과 배지 위치도 함께 조정했다.
